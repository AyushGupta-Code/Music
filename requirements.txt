# Core torch stack (pin to a CPU-friendly build; change as needed for CUDA/MPS).
# Audiocraft 1.4.0a2 targets torch/torchaudio 2.1.0; keep these in sync to avoid
# resolver conflicts during `pip install -r requirements.txt`.
torch==2.1.0
torchaudio==2.1.0

# MusicGen
transformers>=4.31.0,<4.44
safetensors
einops
omegaconf
soundfile
sentencepiece
git+https://github.com/facebookresearch/audiocraft.git

# Voice + audio utilities
# MeloTTS is not on PyPI; install from the official repository
git+https://github.com/myshell-ai/MeloTTS.git
pydub
scipy
unidic-lite

# Local model downloads
huggingface_hub>=0.23

Flask>=3.0
